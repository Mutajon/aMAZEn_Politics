# Amaze Politics - Environment Variables Example
# Copy this file to .env and fill in your actual values

# =============================================================================
# AI Provider Configuration
# =============================================================================

# OpenAI API Key (required for GPT models)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key (optional - for Claude models)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# AI Model Configuration
CHAT_MODEL=gpt-5-mini                    # Model for general chat responses
MODEL_DILEMMA=gpt-5                      # Model for dilemma generation
MODEL_MIRROR=gpt-5                       # Model for mirror/compass interactions
MODEL_ANALYZE=gpt-4o                     # Model for role analysis
IMAGE_MODEL=gpt-image-1                  # Model for avatar generation

# Text-to-Speech Configuration
TTS_MODEL=gpt-4o-mini-tts                # TTS model (gpt-4o-mini-tts supports instructions, tts-1/tts-1-hd for basic)
TTS_VOICE=onyx                           # TTS voice (onyx, echo, fable, onyx, nova, shimmer)
TTS_INSTRUCTIONS=Speak in a mysterious and amused tone, with ethereal echo  # Optional: Style/tone instructions (only for gpt-4o-mini-tts)
TTS_FORMAT=mp3                           # Audio format (mp3, opus, aac, flac)

# Alternate provider (Anthropic) - uncomment to use Claude instead of GPT
# MODEL_DILEMMA_ANTHROPIC=claude-3-5-sonnet-20241022
# MODEL_MIRROR_ANTHROPIC=claude-3-5-haiku-20241022

# =============================================================================
# MongoDB Configuration (Data Collection)
# =============================================================================

# MongoDB Connection String
# Local: mongodb://localhost:27017
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_URI=mongodb://localhost:27017

# MongoDB Database Name
MONGODB_DB_NAME=political-game

# Enable/Disable Data Collection (true/false)
ENABLE_DATA_COLLECTION=true

# Default Treatment (if not assigned)
DEFAULT_TREATMENT=control

# =============================================================================
# Security Configuration
# =============================================================================

# Allowed Origins for CORS (comma-separated list)
# Production: https://your-domain.com,https://www.your-domain.com
# Development: http://localhost:5173,http://localhost:3001
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3001

# =============================================================================
# Error Monitoring (Sentry)
# =============================================================================

# Sentry DSN (from sentry.io project settings)
# Leave empty to disable error monitoring
VITE_SENTRY_DSN=

# Environment name (production, staging, development)
VITE_ENVIRONMENT=development

# App version (automatically read from package.json)
VITE_APP_VERSION=1.0.0

# =============================================================================
# Server Configuration
# =============================================================================

# Node Environment (production, development)
NODE_ENV=development

# Server Port (default: 3001)
PORT=3001
